package sort;
// 삽입정렬
// 인덱스 n+1번부터 n이 0이 될때 까지 순차적으로 정렬
// insert 할 값을 임시변수에 저장
// 그 임시변수에 저장된 값을 insert값의 인덱스 -1 값과 비교하여 그 값이
// insert값보다 크다면 shift하고 insert값보다 작으면 그 자리에 insert값을
// 입력한다.

// 시간복잡도는 최악의 경우는 O(n^2)이다.
// 최악의 경우에는 버블정렬이나 선택정렬과 같지만, 최선의 경우 시간복잡도는 그 절반정도로 줄어들 수 있다.

public class InsertionSort {
    public static void main(String[] args) {

    }
}
