package sort;
// 버블 정렬
// 인접한 값을 비교해서 순차적으로 밀어주는 정렬
// for 문이 2개 중첩되기 때문에 O(n^2)가 된다.

// 최악의 경우, 최고의 경우 시간복잡도 차이가 존재하지 않는다.
// 시간복잡도 O(n^2)

public class BubbleSort {
    public static void main(String[] args) {

    }
}
